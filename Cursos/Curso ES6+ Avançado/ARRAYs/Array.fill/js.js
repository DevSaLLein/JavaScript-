// Criando um array com espaço para 50 valores;
const arr = new Array(50); 

arr.fill('lol' , 3 , 10);

console.log(arr)

/* 
 Array.fill() => {

    -> parâmetro 1: recebe o valor que será inserido;
    -> parâmetro 2: recebe onde começa >Opcional<;
    -> parâmetro 3: recebe onde vai terminar >Opcional<;

 }

*/

